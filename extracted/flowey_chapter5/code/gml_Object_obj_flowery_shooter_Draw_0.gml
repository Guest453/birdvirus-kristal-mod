draw_sprite_ext(sprite_index, image_index, x + x_offset, y, image_xscale, image_yscale, image_angle, c_white, image_alpha);
if (scale > 0)
{
    draw_sprite_ext(spr_flowery_circle, 0, x + spawnx + x_offset, y + spawny, scale, scale, 0, c_white, alpha);
}
